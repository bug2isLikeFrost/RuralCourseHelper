const PROMPT_TEMPLATES = {
  systemPrompt: `你是一位乡村教育专家，擅长将本地乡土资源转化为跨学科课程方案。

【核心原则】
你设计的课程必须同时满足：真实性、可行性、乡土性。

【事实核查要求】
- 所有文学引用必须是真实存在的作品（作者、作品名必须可查证）
- 历史典故必须有据可查，禁止凭空编造
- 科学知识必须符合已知的科学事实
- 如果无法确认某个知识的真实性，必须标注"建议教师自行核实"而非编造

【乡村教师资源边界】
- 禁止要求教师展示：古代文物、博物馆级藏品、专业实验设备
- 允许的展示资源：实物样本、图片视频、简易模型、自制教具、互联网资源
- 禁止要求教师具备：专业艺术技能、实验室操作能力、特殊仪器使用能力
- 鼓励利用：当地自然材料、传统工艺、民间智慧、家中常见物品

【任务可行性标准】
- 任务所需材料必须是乡村环境容易获取的（田野、自家厨房、当地集市）
- 禁止要求收集超过5种特定物品（乡村资源有限）
- 测量任务必须使用简单工具（尺子、秤、量杯、计数器），禁止要求精密仪器
- 任务时间必须在课堂时间（40-45分钟）内可完成
- 禁止假设学生具备：上网查询条件、专业书籍、特殊耗材
- 优先选择：观察、绘画、讲述、计算、简单实验等活动

【参考链接质量标准】
- 链接必须直接指向知识所在页面，而非网站首页
- 优先使用：教育部官网、各省教育厅官网、权威科普网站（如果壳网、中国科普博览）、名师公开课、国家中小学智慧教育平台
- 链接到的页面必须包含课程所需的核心知识
- 每个链接必须是可以直接访问的真实URL，禁止使用占位符
- 如果无法找到合适的权威链接，该字段可为空数组[]

【常见错误警告】
以下内容必须避免：
- ❌ 引用不存在的文学作品（如"范仲淹《油菜花》"）
- ❌ 要求展示博物馆级藏品或古代文物
- ❌ 要求学生使用pH计、糖度计等精密仪器
- ❌ 要求收集10种以上特定物品
- ❌ 提供与内容无关的链接或网站首页链接
- ❌ 假设乡村学校有专业实验室或特殊耗材`,

  generateCourses: function(localThing, type) {
    return `请根据教师提供的本地事物，生成6个跨学科课程方案。

本地事物：${localThing}
素材类型：${type}

输出格式要求：
1. 必须输出有效的JSON数组格式，所有键名和字符串值必须使用英文双引号
2. 每个课程方案必须包含以下字段：
   - 学科：课程所属学科（如：语文、数学、科学、美术、音乐、综合实践、道德与法治等）
   - 课程名称：富有创意和吸引力的课程名称
   - 适合年级：具体年级范围（如：3-4年级）
   - 核心知识点：2-3个主要知识点，必须是可查证的真实知识
   - 活动步骤：JSON数组格式，包含4-5个步骤，每个步骤要详细描述具体操作和引导语，如：["教师带领学生到田间观察稻穗形态，引导学生用手指触摸稻叶的质感", "回到教室后，请学生用形容词描述观察到的稻穗特征", "分发画纸，让学生画出稻穗的结构，标注各部分名称", "分组讨论：稻穗的生长周期与季节有什么关系？", "课后作业：采访家人，了解本地稻米的种植历史"]
   - 所需材料：统一的材料清单，列出完成所有步骤所需的全部材料（如：画纸、铅笔、放大镜、白米样本等，用顿号分隔）
   - 延伸问题：1个启发性思考问题
   - 知识库：详细的备课参考资料（300-400字），包含课程相关的背景知识、教学要点、注意事项等，帮助教师深入理解课程内容，必须基于真实信息
   - 相关链接：一个JSON数组，包含2-3个参考链接，每个链接需要有name（网站名称）和url（真实可访问的完整URL）

3. 课程要体现跨学科融合特点，注重实践性和趣味性
4. 活动步骤必须详细具体，包含教师引导语和学生活动，每个步骤至少20字以上
5. 相关链接必须是真实可靠的资源，且链接必须指向知识所在的具体页面

请直接输出JSON数组，不要包含任何其他文字说明。`;
  },

  parseResponse: function(text) {
    try {
      let jsonStr = text.trim();

      const codeBlockMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/);
      if (codeBlockMatch) {
        jsonStr = codeBlockMatch[1];
      }

      let jsonMatch = jsonStr.match(/\[[\s\S]*\]/);
      if (!jsonMatch || jsonMatch[0].length < 50) {
        const lastBrace = jsonStr.lastIndexOf('[');
        const lastCloseBrace = jsonStr.lastIndexOf(']');
        if (lastBrace !== -1 && lastCloseBrace !== -1 && lastCloseBrace > lastBrace) {
          jsonStr = jsonStr.substring(lastBrace, lastCloseBrace + 1);
          jsonMatch = [jsonStr];
        }
      }

      if (jsonMatch) {
        jsonStr = jsonMatch[0];
      }

      console.log('AI原始内容:', jsonStr.substring(0, 300));

      jsonStr = jsonStr
        .replace(/：/g, ':')
        .replace(/"/g, '"')
        .replace(/'/g, '"')
        .replace(/　/g, ' ')
        .replace(/[\r\n]+/g, ' ')
        .replace(/\s+/g, ' ')
        .trim();

      const fn = new Function('return ' + jsonStr);
      const courses = fn();

      if (Array.isArray(courses)) {
        return courses.slice(0, 6);
      }
      return null;
    } catch (e) {
      console.error('JSON解析失败:', e);
      console.error('原始内容:', text.substring(0, 500));
      return null;
    }
  }
};